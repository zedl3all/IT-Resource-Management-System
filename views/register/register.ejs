<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    
    <link rel="stylesheet" href="/css/register.css">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>

    <div class="container">
        <header class="header">
            <div class="header-logo">
                <span role="img" aria-label="gear">⚙️</span> IT RMS
            </div>
            <nav class="header-nav">
                <a href="#">Dashboard</a>
                <a href="#" class="active">Assets</a>
                <a href="#">Users</a>
            </nav>
        </header>

        <main class="main-content">
            <h1><%= heading %></h1>

            <div class="form-card">
                <form action="/add-asset" method="POST">
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="asset-name">ชื่ออุปกรณ์ (Asset Name)</label>
                            <input type="text" id="asset-name" name="assetName" class="form-control" placeholder="เช่น Dell Latitude 5420" required>
                        </div>

                        <div class="form-group">
                            <label for="asset-type">ประเภท (Asset Type)</label>
                            <select id="asset-type" name="assetType" class="form-control">
                                <% assetTypes.forEach(type => { %>
                                    <option value="<%= type %>"><%= type %></option>
                                <% }); %>
                            </select>
                        </div>

                        <div class="form-group">
                            <label for="serial">หมายเลขซีเรียล (Serial Number)</label>
                            <input type="text" id="serial" name="serialNumber" class="form-control">
                        </div>

                        <div class="form-group">
                            <label for="status">สถานะ (Status)</label>
                            <select id="status" name="status" class="form-control">
                                <% statuses.forEach(status => { %>
                                    <option value="<%= status %>"><%= status %></option>
                                <% }); %>
                            </select>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="notes">หมายเหตุ (Notes)</label>
                        <textarea id="notes" name="notes" rows="4" class="form-control" placeholder="ข้อมูลเพิ่มเติม เช่น วันที่ซื้อ, ผู้รับผิดชอบ..."></textarea>
                    </div>

                    <div class="form-actions">
                        <button type="button" class="btn btn-secondary">ยกเลิก</button>
                        <button type="submit" class="btn btn-primary">บันทึกข้อมูล</button>
                    </div>
                </form>
            </div>
        </main>
    </div>

</body>
</html>